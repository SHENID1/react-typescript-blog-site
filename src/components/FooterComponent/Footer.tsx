import React from 'react';
import cl from "./style.module.css"

const Footer = () => {
    return (
        <div className={cl.footer}>
            это Футер
        </div>
    );
};

export default Footer;